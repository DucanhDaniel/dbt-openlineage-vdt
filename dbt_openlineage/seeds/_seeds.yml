version: 2

seeds:
  - name: province_normalization
    description: "Mapping table to normalize province names"
    columns:
      - name: original_province
        data_type: text
      - name: standardized_province
        data_type: text
